#ifndef UTILS_HPP
#define UTILS_HPP

#include "Server.hpp"

class Utils
{
	public:
		// Check if a user exists 
		int user_exists(std::string nickname);
		// int user_exists(std::string nickname, std::vector<User> users);

	private:
};

# endif // UTILS_HPP